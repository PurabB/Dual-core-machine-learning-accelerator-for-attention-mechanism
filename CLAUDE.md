- Finish Step1-4 of this project, recorded in `./README.md` by using `innovus` and `dc_shell` on `UCSDServer`

- First using `ssh UCSDServer` to log in the server, **then input** `ECE260B_WI26_A00` to activate `innovus` and `dc_shell`
