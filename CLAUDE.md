- First using `ssh UCSDServer` or `ssh UCSDServer10`, `ssh UCSDServer12`, `ssh UCSDServer14` to log in the server

- See if the server is busy. If it's busy, try another one.

- Then input `ECE260B_WI26_A00` to activate `innovus` and `dc_shell` (Remember that the server is interactive)

- When running any PnR or simulation, use `nohup` to make it running in the background

- Make sure all modules can be done in 1 cycle when clock frequency is 1GHz (TSMC65nm)

- Read manual of innovus on the server to make sure you have the right grammer.
